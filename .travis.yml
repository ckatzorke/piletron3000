
language: node_js
node_js:
  - "8"
cache:
  directories:
    - "node_modules"

env:
  - CI=true

before_deploy:
  - npm install netlify-cli -g
  - "ng build --prod && cp dist/index.html dist/200.html"

deploy:
  provider: script
  script: netlify deploy -s $NETLIFY_SITEID -t $NETLIFY_TOKEN -p ./dist
  skip_cleanup: true
  on:
    branch: master
